# 学习进度记录

> 本文档按知识主题分类整理（消息队列、缓存/Redis、MySQL、Elasticsearch、RAG与AI应用、微服务架构、Java基础、JVM），不再按日期罗列，方便按主题查阅和复习。

## 目录

- [一、消息队列（MQ）可靠性设计](#一消息队列mq可靠性设计)
- [二、幂等性设计](#二幂等性设计)
- [三、秒杀场景的预防与限流](#三秒杀场景的预防与限流)
- [四、缓存 / Redis](#四缓存--redis)
- [五、MySQL](#五mysql)
- [六、Elasticsearch](#六elasticsearch)
- [七、RAG 与 AI 应用开发](#七rag-与-ai-应用开发)
- [八、微服务架构（Nacos + OpenFeign）](#八微服务架构nacos--openfeign)
- [九、Java 基础](#九java-基础)
- [十、JVM](#十jvm)
- [待深入 / 后续可以扩展的方向](#待深入--后续可以扩展的方向)

---

## 一、消息队列（MQ）可靠性设计

### 1.1 本地消息表方案

**核心问题**：数据库操作和MQ消息发送是两个独立的资源，无法天然保证"要么都成功、要么都失败"，可能出现"数据库写成功了但消息没发出去"或者反过来的不一致情况。

**解决思路**：
- 不直接在业务代码里调用MQ发送逻辑，而是把"待发送的消息"作为一条记录，和业务数据放在**同一个数据库事务**里一起写入本地消息表
- 借助数据库事务本身的原子性，保证业务数据和消息记录要么一起成功、要么一起回滚
- 真正的MQ发送动作，交给一个独立的**异步任务**去扫描本地消息表里"待发送"的记录，尝试发送，成功后更新状态，失败则重试

**需要配合的点**：
- 下游消费者必须做**幂等处理**，因为重试机制可能导致同一条消息被消费多次
- 本地消息表本身需要定期清理/归档，避免无限膨胀

**其他相关方案（了解，不一定用）**：
- 事务消息（如RocketMQ自带的半消息+回查机制）
- 生产者确认/消费者手动确认（保障MQ传输链路本身不丢消息，和本地消息表配合使用）
- Saga模式（适用于跨多服务的复杂长事务流程）

### 1.2 消息全链路可靠性保障（生产者 → Broker → 消费者）

本地消息表方案，主要解决的是"业务数据和消息是否一致"的问题；这部分补充的是**消息本身在整个传输链路上如何做到不丢失**，两者是配合使用、互相补充的关系。

**1. 生产者 → 队列（Broker）这一段**
- **同步发送 + ACK确认机制**：生产者用同步方式发送消息（阻塞当前流程，等待发送结果返回后才继续往下走），并且要求Broker返回明确的ACK确认，确认消息已经被Broker成功接收，生产者才认为这次发送成功；如果没收到ACK（比如网络异常、超时），生产者可以据此判断需要重发，而不是发完就不管了、默认认为一定成功
- **失败重试**：发送失败（没收到ACK、抛异常）时，生产者要有**重试机制**，按一定策略（比如固定间隔重试、指数退避）重新尝试发送，而不是失败一次就直接放弃；通常还要设置最大重试次数，避免无限重试卡住主流程

**2. 队列（Broker）自身这一段**
- **消息持久化**：Broker收到消息后，不能只放在内存里，要**落盘存入磁盘**，即使Broker本身重启/宕机，消息也不会因为还没消费就丢失
- **集群部署（主从/多副本）**：Broker做成集群，消息在多个节点间有副本，即使某一个节点故障，其他节点上还保留着这条消息，不会因为单点故障导致消息彻底丢失

**3. 队列（Broker）→ 消费者这一段**
- **消费者手动确认（Manual Ack）**：消费者处理完这条消息对应的业务逻辑，**确认业务处理成功之后**，才手动向Broker发送确认（ack），Broker收到确认才会把这条消息标记为已消费、可以清除；如果消费者处理过程中出错、或者还没来得及确认就宕机，Broker没收到确认，会重新把这条消息投递给其他消费者（或者等这个消费者重启后重新投递），保证消息不会因为消费者处理失败就凭空消失
- **失败重投递 + 死信队列（DLQ，Dead Letter Queue）兜底**：消费者处理失败（抛异常、没确认）时，Broker 会把这条消息**重新投递**，但不能无限重投——设定一个**最大重试次数**，超过这个次数依然处理失败，就说明这条消息大概率存在业务逻辑处理不了的问题（而不是偶发的网络抖动能自愈的），此时把这条消息转入专门的**死信队列**，不再自动重试，交由**人工介入排查**，避免一条"处理不了"的消息在正常队列里反复重投，一直占用消费资源、甚至阻塞后面正常消息的消费

**4. 消费者端要做幂等性处理**
因为上面这套"没收到确认就重投"的机制，必然会导致**同一条消息有可能被投递、消费多次**（比如消费者其实已经处理完业务逻辑了，但确认消息在网络传输时丢了，Broker没收到确认，就会重新投递一次），所以消费者处理业务逻辑时，必须做幂等处理，否则会出现重复下单、重复扣库存等问题（具体的幂等性实现方式，详见"二、幂等性设计"章节）。

**5. 最终兜底：本地消息表 + 定时扫描**
上面这一整套"生产者重试、Broker持久化/集群、消费者手动确认+失败重投递+死信队列"，解决的是消息在MQ传输链路内部的可靠性问题；但如果生产者这边**在发送消息之前，业务代码本身就出了问题**（比如进程直接崩溃、还没来得及发送消息），或者重试次数用完了依然没发送成功，仅靠上面这套机制还是可能出现消息彻底没发出去的情况。这时候就要靠前面提到的**本地消息表方案**做最后一道兜底——不管MQ链路这边发送重试多少次、成不成功，业务数据和"待发送"这个记录本身已经借助数据库事务保证落地了，再配合一个**定时任务持续扫描**本地消息表里状态还是"待发送"的记录，即使之前的同步重试全部失败了，也能靠这个定时任务反复补偿重发，直到成功为止，从根本上杜绝"消息彻底丢失、无法追溯"的情况。

**全链路串联起来看**：
```
生产者                    Broker（队列）                消费者
  │                          │                          │
  │──同步发送+等待ACK────────▶│                          │
  │◀──ACK确认收到──────────  │                          │
  │  (没收到ACK就重试发送，   │──消息持久化到磁盘          │
  │   达到最大次数则报警)     │  (可选：集群多副本)         │
  │                          │──投递消息────────────────▶│
  │                          │                          │──处理业务逻辑
  │                          │◀──手动确认(处理成功后)────│
  │                          │  (没收到确认就重新投递，    │
  │                          │   达到最大重试次数         │
  │                          │   → 转入死信队列，人工介入) │
  │                          │                          │──(消费者做幂等，防重复处理)
```

---

## 二、幂等性设计

### 2.1 订单幂等性设计 —— 防止重复下单

**要解决的问题**：同一个下单请求，可能因为网络抖动导致用户重复点击、前端重复提交、或者 MQ 消息重复消费等原因，被多次处理，如果不做幂等控制，会导致重复创建订单、重复扣库存等问题。

**整体流程**：
```
用户点击"下单"
    ↓
① 后端先下发一个一次性 token（由 用户ID + 产品ID 生成）
    ↓
② 用户带着这个 token 真正发起下单请求
    ↓
③ 用 用户ID + 一次性token 拼成 Redis 分布式锁的 key，
   通过 Redis 的 SETNX 命令，把这个 key 的状态设置为 processing（处理中）
    ↓ SETNX 成功（说明这是第一次拿到锁）→ 继续往下走正常下单逻辑
    ↓ SETNX 失败（说明这个 token 已经在被处理，或者已经处理过）→ 直接返回"已使用"，不重复处理
    ↓
④ 数据库层面用唯一索引兜底
   （即使前面的分布式锁因为极端情况失效，数据库唯一索引也能保证不会插入重复订单）
```

**设计要点**：
- **一次性 token**：由 用户ID + 产品ID 生成，保证这个下单请求的唯一性标识，避免同一个用户对同一个商品的下单请求被重复处理
- **Redis SETNX**：作为第一层防护，速度快，能在大部分场景下拦截住重复请求
- **数据库唯一索引**：作为最后一层兜底，即使 Redis 因为某些极端情况（比如锁提前过期、Redis 本身故障）没能拦住，数据库层面依然能保证不会出现重复数据，这是保证幂等性的"最后一道防线"

**体现的设计思想**：多层防护、层层兜底——不完全依赖单一手段（比如只靠 Redis 锁），因为任何单一组件都有可能失效，用"Redis 锁 + 数据库唯一索引"两层组合，兼顾了性能和可靠性。

### 2.2 幂等性处理的通用实现方式

不只是订单场景，MQ消费者去重等各类需要幂等的场景都可以用到，可以配合使用，不是互斥的：
- **唯一业务ID**：给每条消息/每次请求生成一个全局唯一的业务标识（比如订单号、消息ID），是做幂等的基础前提——没有这个唯一标识，后面几种手段都无从谈起
- **数据库唯一索引兜底**：在数据库层面对关键字段（比如订单号、消息ID）建唯一索引，即使业务代码逻辑判断有疏漏、并发下同时插入了两条相同标识的数据，数据库会直接拒绝第二条插入，是幂等性设计里**最后一道、最可靠的防线**
- **本地消息表**：本身也自带幂等属性——消息记录和业务数据在同一个事务里生成，配合消息ID去重判断，可以确认某条消息是否已经处理过，避免重复处理
- **状态机**：给业务数据设计明确的状态流转（比如订单：待支付→已支付→已发货），处理前先检查当前状态是否已经流转到了目标状态之后，如果已经流转过就直接跳过，不重复执行（比如"订单取消时先判断订单状态是否还是待支付，已经不是了就不再重复回补库存"，就是这个思路的具体应用）
- **Redis 的 SETNX**：利用 `SETNX`（或 `SET key value NX`）命令的原子性，尝试给唯一业务ID设置一个标记，设置成功说明是第一次处理，设置失败（key已存在）说明已经处理过或正在处理中，直接跳过；通常还会给这个key设置一个合理的过期时间，避免长期占用

**方案选择建议**：唯一业务ID是前提，必须要有；Redis SETNX 适合做**快速的第一层拦截**，性能好、响应快；数据库唯一索引/状态机判断适合做**最终兜底**，即使Redis这一层因为某些极端情况失效了，数据库层面依然能保证不出现真正的重复数据。

---

## 三、秒杀场景的预防与限流

**秒杀面临的核心问题**：短时间内海量请求集中涌入，远超系统正常承载能力，如果不做防护，容易导致数据库被打垮、库存超卖、服务雪崩。

### 3.1 整体防护思路：层层拦截，越往后能扛住的请求量越少

```
用户请求
    ↓
① 前端层拦截（按钮防抖、点击后置灰，减少无效重复请求）
    ↓
② 网关/接入层限流（挡住大部分明显超量的流量，不让它们进入核心业务系统）
    ↓
③ 业务层限流 + 排队/削峰（进一步控制真正进入业务处理的请求速率）
    ↓
④ 缓存层预扣库存（避免直接打到数据库做库存判断）
    ↓
⑤ 数据库层最终兜底（唯一索引、原子扣减，防止极端情况下的超卖）
```

### 3.2 限流的常见实现方法

**1. 计数器/固定窗口限流**
在固定时间窗口内（比如1秒），限制最多允许通过的请求数，超过就直接拒绝。实现简单，但窗口临界点可能出现瞬时流量翻倍的问题（比如窗口切换的瞬间，前一秒末尾和后一秒开头的请求叠加在一起超过预期）。

**2. 滑动窗口限流**
比固定窗口更精细，把时间窗口拆成更小的区间滚动统计，避免固定窗口临界点的突刺问题。

**3. 漏桶算法（Leaky Bucket）**
请求先进入一个"桶"里，桶以**固定的速率**往外处理请求，不管请求瞬时涌入多少，处理速度是恒定的，超出桶容量的请求直接丢弃。适合需要严格控制处理速率、流量整形的场景。

**4. 令牌桶算法（Token Bucket）**
系统按固定速率往桶里放令牌，请求要处理必须先拿到一个令牌，桶里没令牌就拒绝或等待。和漏桶的区别是**令牌桶允许一定程度的突发流量**（只要桶里存的令牌够，可以短时间内一次性处理多个请求），比漏桶更灵活，也是目前最常用的限流算法（比如 Guava 的 RateLimiter、Sentinel 底层都用到类似思想）。

**常见落地方式**：
- 网关层：Nginx 限流模块、Spring Cloud Gateway 自带的限流过滤器
- 应用层：Sentinel、Guava RateLimiter
- 分布式限流（多实例部署时，要控制的是全局总量而不是单机）：基于 Redis + Lua 脚本实现的分布式令牌桶/计数器

### 3.3 秒杀专属的防护手段

**1. 库存预热到缓存，不直接查数据库**
把秒杀商品的库存提前加载到 Redis 里，请求先在 Redis 层面做库存判断和扣减（用 Redis 的原子操作，比如 `DECR`，或者 Lua 脚本保证判断+扣减的原子性），只有真正抢到名额的请求才会进一步落地到数据库创建订单，避免全部请求直接打到数据库。

**2. 请求排队/异步削峰**
用消息队列把瞬时涌入的大量下单请求先"缓冲"起来，业务处理端按照数据库/系统能承受的速率，从队列里慢慢消费处理，而不是让所有请求同时涌向数据库。

**3. 恶意请求识别与拦截**
比如同一账号/同一IP在极短时间内的重复请求识别、验证码/滑块校验，减少脚本刷单、恶意抢购对真实流量的挤占。

**4. 与其他知识点的呼应**：
- 秒杀下单同样需要"二、幂等性设计"里梳理过的**订单幂等性设计**（一次性token + Redis分布式锁 + 数据库唯一索引兜底），防止同一用户重复抢到多单
- 库存的原子扣减方式，和"用 UPDATE 语句的 WHERE 条件里带上 `stock > 0`"是同一个思路的延伸，只是秒杀场景下会先在 Redis 这一层做一次预判拦截，减少真正打到数据库的请求量

---

## 四、缓存 / Redis

### 4.1 缓存三大经典问题 —— 击穿、穿透、雪崩

这三个问题经常被放在一起讨论，但成因和解决思路各不相同，需要区分清楚。

**1. 缓存穿透（查询的数据压根不存在）**

问题：用户查询一个**数据库里根本不存在**的数据（比如一个不存在的商品ID），缓存里自然也没有，每次都会穿透缓存直接打到数据库，如果被恶意大量请求这种不存在的数据，会把数据库压垮。

解决方法：
- **缓存空结果**：查询数据库后如果发现是空的，也把这个"空结果"缓存起来（设置一个较短的过期时间），下次同样的请求直接从缓存返回空，不再打到数据库
- **布隆过滤器（Bloom Filter）**：提前把所有真实存在的数据ID放进布隆过滤器，请求进来先经过布隆过滤器判断，如果判断"一定不存在"，直接拦截返回，不需要再查缓存和数据库

**2. 缓存击穿（某个热点key突然失效）**

问题：某一个**访问量特别高的热点数据**（比如爆款商品），缓存到期失效的一瞬间，恰好来了大量并发请求，这些请求全部同时穿透到数据库，瞬间给数据库带来巨大压力。

解决方法：
- **加互斥锁（分布式锁）**：缓存失效时，只允许第一个请求去数据库查询并重建缓存，其他请求等待或者返回旧数据，避免所有请求一窝蜂地打到数据库
- **热点数据不设置过期时间，或者设置成永不过期**：由后台任务定期主动更新，而不是依赖被动过期后触发重建
- **提前预热/续期**：在快过期之前，提前用后台任务刷新缓存，避免出现真正的失效空档期

**3. 缓存雪崩（大批量key在同一时间集中失效）**

问题：大量缓存key设置了**相同的过期时间**，到了那个时间点集体同时失效，导致短时间内大量请求同时穿透到数据库，造成数据库瞬间压力过大甚至宕机。

解决方法：
- **给过期时间加上随机值**：避免大量key设置成同一个固定过期时间，比如基础过期时间上再加一个随机的浮动值（比如30分钟±5分钟随机），让失效时间分散开，不会同时集中失效
- **缓存高可用**：给缓存本身（比如Redis）做集群部署、主从/哨兵机制，避免缓存服务本身宕机导致全部请求瞬间打到数据库
- **服务熔断/限流兜底**：即使真的发生了大量缓存失效，也可以在数据库前面加一层限流或熔断保护，防止数据库被彻底打垮，保证核心服务不整体崩溃

**三者对比总结**：

| 问题 | 本质原因 | 核心解决思路 |
|---|---|---|
| 缓存穿透 | 查询根本不存在的数据 | 缓存空结果 + 布隆过滤器提前拦截 |
| 缓存击穿 | 单个热点key突然失效 | 互斥锁重建 + 热点数据不过期/提前续期 |
| 缓存雪崩 | 大批量key同时失效 | 过期时间加随机值 + 缓存高可用 + 限流熔断兜底 |

### 4.2 Redis 的删除策略与淘汰策略

这两者解决的是不同的问题：**删除策略**解决"过期的key什么时候真正从内存里清掉"，**淘汰策略**解决"内存不够用了，该优先舍弃哪些key腾出空间"。

**删除策略（过期key怎么删）**
- **惰性删除**：key过期后不会立刻被清理，还是留在内存里，只有等**下次有请求查询到这个key时**，才检查发现已过期并真正删除。优点是不用额外浪费CPU去主动扫描，缺点是如果这个key之后一直没人查询，会一直占用内存不被释放
- **定期删除**：Redis 后台**定期**（默认每秒执行若干次）随机抽取一部分设置了过期时间的key，检查是否过期，过期的直接删除。优点是能主动清理一部分不会再被访问的过期key，避免惰性删除"没人查询就一直占内存"的问题；缺点是只是抽样检查，不可能一次性覆盖所有过期key
- **实际使用**：**惰性删除 + 定期删除两者结合使用**，实际业务场景里都会用到——定期删除负责兜底清理大部分过期数据，惰性删除负责补漏（万一某个过期key恰好没被定期删除的抽样命中，用户访问时依然能被正确判断为已过期，不会读到脏数据）

**淘汰策略（内存不够时，优先淘汰谁）**
当 Redis 内存达到设定上限，需要腾出空间时，会按配置好的淘汰策略决定删除哪些key：
- **`volatile-lru`**：只在**设置了过期时间**的key中，淘汰最久没有被使用的（LRU，Least Recently Used）
- **`volatile-lfu`**：只在**设置了过期时间**的key中，淘汰使用频率最低的（LFU，Least Frequently Used）
- **`volatile-random`**：只在**设置了过期时间**的key中，随机淘汰
- **`volatile-ttl`**：只在**设置了过期时间**的key中，淘汰剩余过期时间最短的（最快要过期的优先被淘汰）
- **`allkeys-lru`**：在**所有key**（不管有没有设置过期时间）中，淘汰最久没有被使用的
- **`allkeys-lfu`**：在**所有key**中，淘汰使用频率最低的
- **`allkeys-random`**：在**所有key**中，随机淘汰

**记忆规律**：`volatile` 开头的策略，淘汰范围只限定在"带过期时间的key"里挑；`allkeys` 开头的策略，淘汰范围是"所有key"，不管有没有设置过期时间都可能被选中。策略后缀（`lru`/`lfu`/`random`/`ttl`）决定的是具体挑选逻辑——按最久未使用、按使用频率、随机、按剩余存活时间。

### 4.3 Redis 与 MySQL 数据同步机制

数据库是权威数据源，Redis 是缓存，两者之间必须有机制保证数据变更后能同步更新/失效，否则会出现缓存和数据库数据不一致的问题。常见的几种方案：

**1. 先更新数据库，再删除缓存**
最常见的基础做法。更新表数据成功后，直接删除对应的缓存key（而不是更新缓存），下次查询时缓存未命中，会重新从数据库加载最新数据写入缓存。删除比更新缓存更简单可靠，避免了"更新缓存的值算错"的情况。

**2. 延迟双删**
在"先更新表、再删缓存"的基础上，进一步防止并发场景下的旧数据被重新写回缓存：
```
① 删除缓存
② 更新数据库
③ 异步延迟一段时间后，再删一次缓存
```
第③步的"延迟再删一次"是为了兜底：防止在①②之间的时间差里，有其他请求读到了旧数据又把旧数据写回了缓存，延迟删除能清掉这种脏数据。

**3. 缓存设置过期时间（兜底策略，不管用哪种方案都应该加上）**
不管前面用了哪种同步机制，缓存都应该设置一个合理的过期时间，作为最后一道保险——即使前面的同步机制出于某些极端原因失效了，缓存到期后也会自动失效，重新从数据库加载，不会让脏数据永久停留在缓存里。

**4. 监听 binlog + 消息队列，保证最终一致性（适合多入口写入的场景）**
如果数据库存在多个不同入口都可能修改（比如后台管理系统、其他内部系统都能改商品数据），很难要求每个入口都记得手动去删缓存。这种情况下可以用第三方工具（比如 Canal）监听数据库的 binlog 变更，一旦检测到数据变化，就发一条消息到消息队列，由专门的消费者统一负责更新/删除对应的缓存，不用每个业务入口都各自处理缓存同步逻辑，从架构上保证不会有遗漏。

**5. 对强一致性要求高的场景，直接读数据库，不走缓存**
个别对数据一致性要求非常高、不能有任何滞后的场景（比如库存扣减这类），可以选择不使用缓存、直接读数据库，但因为跳过了缓存这层"缓冲"，需要额外做好并发控制（比如限流），防止大量请求直接压垮数据库。

**6. Redis 存版本号+时间戳，定时扫表比对，不一致直接失效**
Redis 缓存里除了业务数据，额外存一个版本号或时间戳字段，用定时任务定期扫描数据库和缓存的版本号/时间戳是否一致，不一致就让缓存失效，等待下次查询时重新加载。这种方案实时性较差（依赖定时任务的扫描周期），适合对一致性要求比较宽松、能接受一定延迟的场景。

**方案选择小结**：单一入口的常规业务，用"先更新表、再删缓存"+ 过期时间兜底就基本够用；多入口写入的复杂场景，upgrade 到监听 binlog + 消息队列的方案；强一致性要求的场景，果断放弃缓存直接读库，用限流等手段保护数据库。

### 4.4 Redis 持久化机制

Redis 数据默认存在内存里，一旦重启/宕机数据会丢失，需要持久化机制把数据落地到磁盘，重启后能恢复。主要有两种方式：

**1. RDB 快照（RDB Snapshot）**

在某个时间点，把内存里的全部数据生成一份快照文件，保存到磁盘。

- **触发方式**：
  - `SAVE`：由主线程同步执行快照，执行期间会**阻塞**主线程，线上生产环境基本不用
  - `BGSAVE`：**fork 一个子进程**去做快照操作，不阻塞主线程，是最常用的方式（也是定时自动快照默认采用的机制）
- **fork 子进程为什么不阻塞主线程**：利用操作系统的**写时复制（Copy-On-Write）**机制，fork 出来的子进程和主进程共享同一份内存数据，只有当主进程后续要修改某块内存数据时，才会把这块内存复制一份出来单独修改，子进程用来做快照的那部分内存内容不受影响，所以主线程可以继续正常处理请求，不需要等快照做完
- **全量+增量的思路**：通常做法是先做一次完整的全量快照，后续可以在此基础上结合其他机制（比如AOF）做增量记录，减少每次都要重新生成全量快照的开销
- **优点**：文件体积相对紧凑，恢复速度快（直接加载快照文件即可）
- **缺点**：快照是"某个时间点"的数据，两次快照之间如果发生宕机，这期间的数据会丢失，数据安全性不如AOF

**2. AOF（Append Only File，追加日志方式）**

把每一条写命令，都以日志追加的方式写入到文件里，重启时通过**重新执行一遍这些命令**来恢复数据。

- **写入策略**（数据安全性和性能的权衡）：
  - `always`：每条命令都立刻同步写入磁盘，最安全但性能最差
  - `everysec`：每秒同步一次，性能和安全性折中，是默认推荐配置
  - `no`：由操作系统自行决定何时同步，性能最好但最不安全
- **优点**：数据安全性更高，最多只丢失极短时间内的数据（取决于同步策略）
- **缺点**：文件会随着写命令的增多不断膨胀，恢复速度相对较慢（需要重新执行大量命令）
- **AOF 重写机制**：当日志文件增长到一定程度，Redis 会触发 AOF 重写，把当前内存里的实际数据状态，压缩成一份最精简的命令集合重新写入，避免文件无限膨胀（比如同一个key被反复SET了很多次，重写后只保留最终这一条结果对应的命令，而不是保留所有历史操作记录）

**两种方式对比**：

| 维度 | RDB | AOF |
|---|---|---|
| 数据安全性 | 较低（两次快照之间的数据可能丢失） | 较高（最多丢失极短时间数据） |
| 文件体积 | 小，紧凑 | 相对较大 |
| 恢复速度 | 快 | 相对较慢 |
| 性能开销 | fork子进程做快照，配合写时复制，开销较小 | 取决于同步策略，`always`开销大 |

**实际生产环境**：通常两种方式**同时开启**，互补使用——RDB 用于快速全量恢复和备份，AOF 用于保证数据尽可能少丢失，Redis 重启时会优先使用 AOF 文件来恢复数据（因为更完整），只有在 AOF 不可用时才会退回用 RDB 文件恢复。

### 4.5 Redis 主从复制

**原理**：
```
从库首次连接主库
    ↓
主库生成一份 RDB 快照，发送给从库，从库加载这份快照，完成一次全量数据同步
    ↓
全量同步完成后，主库后续接收到的写命令，会以类似 AOF 日志的方式，
持续增量地同步给从库，从库不断重放这些命令，保持和主库数据一致
```
也就是说，主从复制的过程，正好结合利用了前面提到的 **RDB（负责首次全量同步）+ AOF 增量日志（负责后续持续同步）** 这两种持久化机制的思路。

**主从复制的作用**：
- **负载均衡**：读请求可以分散到多个从库上处理，减轻单台机器的压力
- **读写分离**：写操作只在主库上执行，读操作分散到从库，主库专注处理写入，从库专注处理查询，互不干扰，提升整体吞吐能力
- **预防宕机（高可用）**：如果主库宕机，可以从从库里选一个提升为新的主库（配合哨兵机制 Sentinel 做自动故障转移），保证服务不会因为单点故障而完全不可用

### 4.6 Redis 哨兵模式（Sentinel）

**作用**：保证 Redis 集群的**高可用**——主从复制能做负载均衡、读写分离，但如果主库宕机，写操作就没地方处理了，需要有机制**自动**把某个从库提升为新的主库，而不是依赖人工手动介入切换，这就是哨兵模式要解决的问题。

**核心流程**：
```
哨兵（Sentinel）集群持续监控主库和各个从库的健康状态
    ↓
主库宕机（哨兵检测到主库无法正常响应）
    ↓
哨兵集群之间进行选举，选出一个"主哨兵"（Leader）负责主导这次故障转移
    ↓
主哨兵在现有的从库里，按一定规则（比如数据最新、优先级配置等）选出一个从库
    ↓
执行故障转移：把选中的这个从库提升为新的主库
    ↓
通知其他从库改为向这个新主库同步数据，同时通知客户端/应用方新的主库地址
```

**几个关键点**：
- **哨兵本身也是集群部署**（多个哨兵实例），不是单个哨兵去做判断和决策，这样即使个别哨兵自己出问题，也不会影响整体的故障检测和转移能力，避免"监控者自己变成单点故障"
- **哨兵之间需要选举出主哨兵**，来主导这次故障转移操作，避免多个哨兵各自为政、同时发起故障转移导致混乱
- **故障转移的本质，是从库到主库身份的切换**，背后依赖的还是**主从复制**机制（新主库本来就是从库之一，数据已经通过复制保持同步，切换后其他从库改为向它同步即可）
- 除了检测"主库彻底宕机"，哨兵也会持续监控整个集群的健康状态，是保证Redis高可用架构里不可或缺的一环

**和其他知识点的串联**：主从复制解决的是"数据怎么同步、怎么做读写分离"，哨兵解决的是"主库挂了怎么办、怎么自动恢复"，两者组合起来，才构成一套相对完整的Redis高可用方案。

### 4.7 Redis 为什么这么快？

- **完全基于内存操作**，读写速度远超磁盘
- **单线程模型**（核心命令处理），避免了多线程上下文切换和锁竞争的开销
- **高效的数据结构**（如跳表、压缩列表等针对不同场景做了优化）
- **I/O 多路复用**（epoll 等），单线程也能高并发处理网络请求

### 4.8 Redis 是单线程还是多线程？

- **核心的命令执行是单线程的**（这也是为什么单条命令具有原子性）
- 但 **Redis 4.0+** 引入了**异步线程**处理一些耗时操作（如大 key 的删除 `UNLINK`）
- **Redis 6.0+** 引入了**多线程处理网络 IO**（读写 socket、协议解析），但命令的实际执行依然是单线程，目的是提升高并发下的网络处理吞吐量

---

## 五、MySQL

### 5.1 InnoDB 引擎特性

- **行级锁**：锁粒度细，并发性能好（相比表锁，不会因为一行数据的操作锁住整张表）
- **支持 MVCC（多版本并发控制）**：解决读写互相阻塞的问题
  - 原理：每行数据隐藏了版本相关的字段（事务ID、指向上一版本的回滚指针），通过这些隐藏字段和 **Read View** 机制，判断当前事务应该看到哪个版本的数据
  - **读**（普通的 `SELECT`，即"快照读"）不加锁，直接读取符合当前事务可见性规则的历史版本数据，不会和写操作互相阻塞
  - **写**（`UPDATE`/`DELETE`/加了 `FOR UPDATE` 的 `SELECT`，即"当前读"）仍然需要加锁，写写操作之间无法通过 MVCC 避免冲突，该加锁还是要加锁
- **支持外键**：可以定义表之间的引用完整性约束
- **支持事务**，满足 ACID 四大特性（详见下面单独展开）
- **索引结构使用 B+ 树**（详见下面单独展开）

### 5.2 事务 ACID 四大特性

**A - 原子性（Atomicity）**
通过 **undo log（回滚日志）** 实现。事务执行过程中，每做一次修改，都会先记录一条对应的"反向操作"到 undo log 里；如果事务执行失败或主动回滚，就靠 undo log 把数据还原到事务开始之前的状态，保证一个事务里的多个操作要么全部生效，要么全部撤销。

**D - 持久性（Durability）**
通过 **redo log（重做日志）** 实现。事务提交时，先把这次修改对应的操作记录写入 redo log（顺序写，速度快），即使数据库随后发生宕机、断电，重启时也能通过重放 redo log 里的记录，把还没来得及刷回磁盘数据文件的修改重新应用一遍，保证已提交事务的修改不会丢失。

**I - 隔离性（Isolation）**
靠 **MVCC + 锁** 共同实现，对应四种隔离级别（隔离程度从低到高）：
1. **读未提交（Read Uncommitted）**：能读到其他事务还没提交的修改（脏读），隔离性最弱，基本不用
2. **读已提交（Read Committed）**：只能读到其他事务已经提交的修改，解决了脏读，但同一事务内多次读取同一数据结果可能不一致（不可重复读）
3. **可重复读（Repeatable Read）**：同一事务内，多次读取同一数据结果保持一致，**MySQL 默认隔离级别**；InnoDB 在这个级别下通过 **MVCC + 间隙锁（Gap Lock）/ Next-Key Lock**，在很大程度上也解决了幻读问题（不是靠 MVCC 单独解决的，是 MVCC 配合锁机制一起解决）
4. **串行化（Serializable）**：所有读写操作都退化成加锁排队执行，隔离性最强，并发性能最差

**C - 一致性（Consistency）**
指数据库的状态始终满足预先设定的业务规则和约束（比如主外键约束、唯一性约束、转账前后总金额不变这类业务逻辑约束）。**一致性是事务追求的最终目标/结果，而不是靠单独某一种日志或锁机制单独实现的**——它是原子性、隔离性、持久性这三者共同配合，再加上数据库本身的约束校验（外键、唯一索引、检查约束等）机制，共同保证达成的结果。可以理解为：A、I、D 是手段，C 是通过这些手段最终达成的目的。

### 5.3 索引：B+ 树

- InnoDB 索引结构使用 **B+ 树**：所有数据都存储在叶子节点，非叶子节点只存索引键值，不存数据，且叶子节点之间用指针横向连接，适合范围查询和顺序扫描，磁盘IO效率高（B+树层级低，一次磁盘IO能加载更多索引信息）
- **聚簇索引（主键索引）**：叶子节点直接存储整行数据，一张表只有一个聚簇索引（通常是主键）
- **二级索引（普通索引）**：叶子节点存储的是主键值，而不是整行数据；通过二级索引查到主键后，如果还需要查询该索引没有覆盖到的其他字段，需要再拿这个主键值去聚簇索引里查一次完整数据，这个过程叫**回表**
- 补充概念：**覆盖索引**（查询所需字段都能在二级索引里直接拿到，不需要回表，性能更好）；**联合索引的最左前缀原则**（建了 `(a, b, c)` 联合索引，查询条件必须从最左边的 `a` 开始用，才能有效利用索引）

**MySQL 为什么用 B+ 树，而不是红黑树**：核心原因是两者的树高不同，导致所需的磁盘IO次数差距很大。
- **B+ 树是多叉树**：一个节点可以存储**大量的 key**（不止一个），MySQL 里**一个节点对应一个数据页**（磁盘读写的最小单位就是数据页，每次读写都是整页读取）。因为一个节点/一页就能存很多个 key，同样数量级的数据，B+树的**树高很低**（比如千万级数据，B+树可能只需要3-4层），一次查询只需要经过很少的几次数据页读取（也就是几次磁盘IO），就能定位到目标数据
- **红黑树是二叉树**：每个节点**只能存一个 key**，同样数量级的数据下，红黑树的**树高会非常高**，一次查询需要从根节点开始逐层往下找，树有多高就需要经过多少层节点——如果每个节点对应一次磁盘IO，树越高意味着磁盘IO次数越多，而磁盘IO本身是相对昂贵、耗时的操作（相比内存操作慢几个数量级）
- **一句话总结**：MySQL 的性能瓶颈很大程度在于磁盘IO次数，B+树"矮胖"的多叉结构，能让一次查询用尽量少的磁盘IO次数覆盖尽量多的数据范围；红黑树"高瘦"的二叉结构，虽然在内存里查找效率也不差（时间复杂度都是O(logN)），但换算到磁盘IO次数上，代价明显更高，所以数据库索引结构普遍选择B+树（或者B树的变种），而不是红黑树这类内存数据结构里常见的平衡二叉树

### 5.4 锁机制

- **按锁的粒度分**：
  - **行锁**：锁定单行数据，粒度最细，并发度最高，InnoDB 默认支持
  - **表锁**：锁定整张表，粒度粗，并发度低，MyISAM 引擎主要靠表锁
  - **页锁**：介于行锁和表锁之间的锁粒度，锁定的是数据页
- **按锁的性质分**：
  - **共享锁（S锁，Shared Lock）**：也叫读锁，多个事务可以同时持有同一份数据的共享锁，互不冲突，但和排他锁互斥
  - **排他锁（X锁，Exclusive Lock）**：也叫写锁，同一时刻只能有一个事务持有，且和其他任何锁（包括共享锁）都互斥
- **补充：意向锁（Intention Lock）**：InnoDB 特有，是一种表级锁，用来快速判断表内是否已经有某种行锁存在，避免每次加表锁时都要遍历检查表内每一行是否被锁，提升加锁效率（意向共享锁 IS、意向排他锁 IX）
- **补充：间隙锁（Gap Lock）和 Next-Key Lock**：InnoDB 在可重复读隔离级别下，为了防止幻读，除了锁住已有的行，还会锁住"行与行之间的间隙"，防止其他事务在这些间隙里插入新数据；Next-Key Lock 是"行锁 + 间隙锁"的组合，是可重复读级别下默认使用的锁类型
- **补充：乐观锁 vs 悲观锁**（这是一种编程层面的思想，不是数据库自带的锁类型）：
  - 悲观锁：假设一定会发生并发冲突，操作前先加锁（对应上面这些数据库锁）
  - 乐观锁：假设冲突概率低，不加锁，而是在更新时用**版本号**或者**条件判断**（比如商城库存扣减时在 WHERE 条件里带上 `stock > 0`）来判断这期间数据有没有被别人改过，这也是商城库存扣减场景实际用到的思路

### 5.5 日志体系

- **undo log（回滚日志）**：记录数据修改前的样子，用于事务回滚（保证原子性），也是 MVCC 实现多版本读取的基础（历史版本数据部分来源于 undo log）
- **redo log（重做日志）**：记录数据修改后的样子，用于崩溃恢复（保证持久性），采用顺序写、先写日志再写数据文件磁盘（WAL，Write-Ahead Logging）的思路，比直接随机写数据文件快得多
- **binlog（归档/复制日志）**：Server 层的日志（undo log、redo log 是 InnoDB 引擎层的日志），记录所有对数据库产生修改的逻辑操作，主要用途是**主从复制**（主库把 binlog 发给从库进行同步）和**数据恢复/审计**
- **relay log（中继日志）**：**主从复制场景下，从库专用**的日志——从库把从主库拉取过来的 binlog 先写入本地的 relay log，再由从库自己的回放线程读取 relay log 里的内容重新执行一遍，从而更新从库自己的数据，是主从复制链路上"从库侧"的中间环节
- **两阶段提交（Two-Phase Commit）**：**保证 redo log 和 binlog 这两份日志内容一致**的机制（因为一次事务提交，既要写 redo log 又要写 binlog，如果两者不一致，主从复制或者崩溃恢复时就可能出现数据不一致）：
  ```
  第一阶段（prepare）：redo log 写入并标记为 prepare 状态（还未真正提交）
      ↓
  写 binlog
      ↓
  第二阶段（commit）：binlog 写入成功后，把 redo log 标记为 commit 状态，事务真正算提交完成
  ```
  如果在写 binlog 之前系统崩溃，恢复时发现 redo log 是 prepare 状态但没有对应的 binlog，会回滚这个事务；如果 binlog 已经写完但 redo log 还没来得及标记 commit 就崩溃，恢复时会检查 binlog 认为这个事务应该是完整的，继续提交，从而保证两份日志的一致性。

### 5.6 MySQL 优化

**1. 用 `EXPLAIN` 排查是否走索引**
执行 `EXPLAIN` 分析SQL的执行计划，重点关注这几个字段：
- `type`：访问类型，判断是索引扫描还是全表扫描（`const`/`ref`/`range` 等较好，`ALL` 表示全表扫描，需要优化）
- `key`：实际使用到的索引，为 `NULL` 说明没走索引
- `rows`：预估需要扫描的行数，越少越好
- `Extra`：额外信息，比如出现 `Using filesort`（需要额外排序）、`Using temporary`（用到了临时表）都是需要关注优化的信号

**2. 常见导致索引失效的原因**
- 索引字段上使用了**函数操作**（比如 `WHERE YEAR(create_time) = 2026`，会导致索引失效，因为存的是索引原值，函数处理后的值不在索引里）
- 查询条件存在**隐式类型转换**（比如字段是字符串类型，但查询时传入的是数字，MySQL会做隐式转换，导致索引失效）
- **`LIKE` 通配符 `%` 放在最左边**（比如 `LIKE '%关键词'`），索引是按前缀有序存储的，最左边不确定就没法利用索引的有序性；`LIKE '关键词%'`（`%`只在右边）是可以走索引的
- **违反联合索引的最左前缀原则**：建了 `(a, b, c)` 联合索引，查询条件没有从 `a` 开始用，索引失效或者只能部分利用
- **`OR` 条件中有字段没建索引**：只要 `OR` 两边有一个条件的字段没有索引，整个查询就可能放弃索引改为全表扫描
- **索引字段参与了运算**（比如 `WHERE age + 1 = 20`），同函数操作类似，索引失效
- **否定条件**（`!=`、`<>`、`NOT IN` 等）大概率不走索引，因为否定条件很难利用索引的有序性去快速定位范围
- **联表查询（JOIN）中，关联字段的字符集或排序规则（collation）不一致**，会导致MySQL无法直接利用索引做关联比较，需要额外转换，索引失效

**3. `SELECT *` 也会拖慢查询**
不仅仅是"多传输了不需要的字段"这么简单，更关键的是容易导致**无法利用覆盖索引**（如果按需只查索引里已有的字段，可以直接从索引获取，不需要回表；一旦用 `SELECT *`，只要有一个字段不在索引里，就必须回表查完整行数据），失去了覆盖索引带来的性能优势。

**4. 深分页问题**
`LIMIT 9000, 100` 这种写法，MySQL 实际上会先扫描前 9000+100 条记录，再丢弃前面的9000条，只返回最后100条——**偏移量越大，浪费的扫描越多，性能越差**。

优化方案：**游标分页（也叫延迟关联/keyset分页）**——记住上一页最后一条记录的 `id`，下一页查询时用 `WHERE id > 上一页最后的id LIMIT 100`，这样不管翻到第几页，都是直接从索引定位到对应位置开始扫描，不需要像 `OFFSET` 那样把前面的数据都扫一遍再丢弃，性能不会随着页数增加而下降。缺点是不能像传统分页一样随意跳转到任意页码，只适合"下一页"这种线性翻页场景。

**5. JOIN 关联表过大的优化**
- **小表驱动大表**：让数据量小的表作为驱动表（放在JOIN左边/外层循环），数据量大的表作为被驱动表，这样可以减少总的匹配扫描次数（每次用小表的一行去大表里查找匹配，比反过来扫描次数更少）
- **确保关联字段都建好索引**，避免JOIN时某一边要做全表扫描

**6. 子查询最好改写成 JOIN**
子查询（尤其是嵌套的 `IN` 子查询）在很多MySQL版本的优化器处理上不如JOIN高效，容易出现每次外层循环都要重新执行一次子查询的情况；改写成JOIN，通常能让优化器更好地利用索引，生成更优的执行计划。

**7. 数据量过大时的架构级优化**
- **分库分表**：把数据水平或垂直拆分到多个库/多张表，单表数据量降下来，单次查询效率自然提升（拆分带来跨库查询、分布式事务等新的复杂度，需要综合权衡）
- **架构层面调整**：读写分离（主库写、从库读，主从复制正是这个思路的基础）、引入缓存层（减少直接打到数据库的请求量，前面Redis相关的缓存设计）

---

## 六、Elasticsearch

### 6.1 ES 在商城搜索里的实际应用流程

**整体查询链路**：
```
用户发起搜索请求
    ↓
① 先查缓存（比如 Redis）
    ↓ 缓存命中 → 直接返回结果
    ↓ 缓存未命中
② 查询 Elasticsearch，用关键词检索，只取出匹配的商品 ID 列表
    ↓
③ 拿着这些 ID，回源查询 MySQL，获取完整的商品详情数据
    ↓
④ 结果写入缓存，返回给用户
```

**设计要点**：
- ES 里通常不存完整的商品数据，只做检索、返回匹配的 ID，真正的详细数据以 MySQL 为准，这样可以避免 ES 和 MySQL 两边数据不一致时，用户看到的详情有问题
- 这个思路和RAG部分"双存储架构"（原文/元数据存 MySQL，检索用的数据单独存一份）逻辑上是一致的：**搜索引擎负责"快速定位"，数据库负责"权威详情"**

**后续优化点（数据量增大后需要考虑）**：
- 需要在 ES 查询和 MySQL 回源查询这两步都加上**分页**，避免一次性查出过多 ID、或者一次性从 MySQL 查出过多明细数据，导致内存压力和响应变慢
- 分页参数（页码、每页数量）需要在两层查询之间保持一致传递

### 6.2 ES 与 MySQL 数据同步机制

商品信息在 MySQL 里发生变更（比如改价格、改库存、上下架）后，需要一种机制把这个变化同步更新到 ES 索引里，否则会出现"数据库已经改了，但搜索出来的还是旧数据"的问题（具体的同步方案细节，可以参考"四、缓存/Redis"部分"Redis与MySQL数据同步机制"里提到的监听binlog+消息队列思路，原理是相通的）。

---

## 七、RAG 与 AI 应用开发

### 7.1 RAG（检索增强生成）基础

**RAG 是什么**：RAG = Retrieval（检索）+ Augmented（增强）+ Generation（生成）。核心目的是让大模型能基于私有的、实时的知识库内容来回答问题，而不是只依赖训练时"记住"的知识，避免编造（幻觉），也能覆盖训练数据里没有的私有信息。

**整体流程分两条线**：
```
【离线】知识库构建
原始文档 → 文档加载(提取纯文本) → 文本切分(Chunking) → 向量化(Embedding) → 存入向量数据库
    同时：原文/元数据存 MySQL，向量/切片存向量库，两边通过 chunk_id 关联（双存储架构）

【在线】用户提问
用户问题 → (可选：结合历史对话做问题改写) → 问题向量化
    → 向量库检索最相关的 topK 片段(可加 metadata 过滤，如租户ID、等级)
    → 组装 Prompt（系统提示词 + 历史记忆 + 检索到的资料 + 当前问题）
    → LLM 生成回答 → 存入记忆 → 返回给用户
```

**几个重点概念**：
- **Embedding 模型 vs ChatModel 是两个不同的模型**：Embedding 负责把文字转成向量用于检索；ChatModel 负责基于检索结果生成自然语言回答。入库和检索阶段必须用**同一个** Embedding 模型，否则向量空间不一致，检索会完全不准
- **多租户隔离**：向量检索本身不会自动按租户过滤，必须在检索时显式加 `tenant_id` 等 metadata 过滤条件，否则会有跨租户数据泄露风险

**RAG 的评判标准**：
- 检索层面：召回率、精确率、排序质量
- 生成层面：忠实度（不编造/幻觉）、答案相关性、完整性、**拒答的合理性**（知识库没有的内容要如实说明，而不是硬编）
- 业务层面：问题解决率、响应速度、引用可追溯性、成本控制
- 量化工具：RAGAS 框架、人工构建测试集做 A/B 对比

**RAG 不适合的场景**：需要复杂推理/多步计算、知识库本身质量差、纯创造性写作、极简单的通用常识问题、对响应速度要求极苛刻的场景。

### 7.2 文本切分（Chunking）详解

**一个容易忽略的前置认知**：文档加载/解析阶段（比如用 PDF、Word 解析库处理原始文件），解析库输出的片段通常是**按文档本身的结构**划分的（比如按标题、按段落、按章节），但每个片段的**长度是不受控的完整长度**——一个章节可能只有几十字，也可能有几千字，解析库本身不会帮你控制片段长度。所以**不能直接把解析库输出的结构化片段原样存入向量库**，还需要自己在这基础上**再写一层切分逻辑**，对超长的片段做二次拆分（比如递归式切分、固定窗口兜底），确保最终存入向量库的每个片段长度合理，这一步是需要自己实现的，不是解析库自带的能力。

**优化思路：用 LLM 辅助文档解析，输出带类型标注的结构化数据，再配合策略模式选择切分方式**
```
原始文档
    ↓ 用 LLM 解析（约束输出固定格式，比如JSON，每个片段带上"类型"字段：faq / paragraph / table / word_entry 等）
得到一批"结构化 + 带类型标注"的数据
    ↓ 根据每条数据的类型字段，用策略模式（Strategy Pattern）动态选择对应的切分策略
    - type=faq        → 走"不切分，整条保留"策略
    - type=paragraph   → 走"递归式切分"策略
    - type=word_entry  → 走"不切分，整体存入"策略
    ↓
执行对应策略处理后，统一存入向量数据库
```
**这么做的好处**：
- 让 LLM 承担"识别内容类型"这个相对模糊、依赖语义理解的判断工作（比原始纯规则判断更准确，比如自动识别出哪些内容属于问答对结构、哪些是普通说明性段落）
- 用**策略模式**把"不同类型对应不同切分逻辑"解耦成独立的策略类，新增一种内容类型/切分方式时，只需要新增一个策略实现类，不需要改动已有的判断逻辑，符合开闭原则，代码更易维护和扩展

**为什么重要**：切分是"检索时能不能找到完整、准确语义单元"的关键一步。
- 切得太粗（一大段作为一个片段）：一个片段混杂多个话题，检索出来噪音多，真正相关的内容被淹没；还可能超出模型输入长度限制
- 切得太碎（切得过于零散）：一句完整的话被拦腰切断，语义残缺不全，检索时可能只命中其中一半信息

**常见切分策略**：

1. **固定字符窗口 + 重叠滑窗**（最简单、最通用）
   - 按固定字符数切（比如500字符一段），相邻片段之间保留一部分重叠（比如50字符）
   - 重叠的作用：即使切分点恰好落在一句话中间，重叠区域也能帮助保留语义连续性，不至于完全割裂
   - 窗口大小设置：太小装不下完整语义单元，太大则片段里混杂太多不相关内容；一般英文300-800字符，中文可以适当更小
   - 重叠长度：一般是窗口大小的10%-20%，太少起不到缓冲作用，太多会导致大量重复内容浪费存储、干扰检索排序
   - 缺点：不理解文档结构，可能把不相关内容拼在一起，也可能切断关键句子；对结构化数据不友好

2. **按语义结构切分**（效果更好，适合有明确结构的文档）
   - 按标题/小节/段落切，保证每个片段是语义完整的单元
   - 对Markdown、HTML这类有结构标记的文档效果更好

3. **递归式切分**（结合结构和长度双重限制，比较推荐的通用方案）
   - 先按大结构单元切（比如按段落），如果某段落还是太长超过限制，再递归地按句子/字符做二次切分
   - LangChain4j 的 `DocumentSplitters.recursive(500, 50)` 用的就是这个思路

4. **不切分，整体存入**（适合结构化数据）
   - FAQ问答对：一问一答本身就是完整语义单元，强行切分会导致答案被拆散、信息残缺
   - 词汇类数据（释义、词性、考试分类、例句）：应整合成一条完整记录整体存入，避免"只知道一部分信息"的情况（比如切分不当会导致检索时只命中释义部分，命中不到考试分类部分）

**按内容类型选择切分策略的对照表**：

| 内容类型 | 切分建议 |
|---|---|
| FAQ问答对 | 不切分，整条保留 |
| 词汇/结构化记录 | 不切分，一条记录整体存入 |
| 语法知识点讲解 | 按知识点切分，不要把定义和例句拆到不同片段 |
| 长篇课文/阅读材料 | 按段落/语义单元切分，配合适当重叠 |
| 政策类条款文档 | 按条款/规则切分，一个条款一个片段 |
| 结构不明显的长文本 | 递归式切分（结构优先，超长再固定窗口兜底） |

**切分粒度与 topK 的配合关系**：
- 切得较碎（片段小）→ 通常需要调大 topK，多取几条片段拼凑完整信息
- 切得较整（像FAQ、词汇那样）→ topK 可以设置得小一些，因为一条记录本身信息已经比较完整

**客服/RAG商城场景的实践经验**：
- 客服场景应优先把知识库整理成 FAQ 格式，检索精度最高；如果原始资料是长文档（比如政策PDF），可以先用 LLM 辅助把长文档提炼成结构化的问答对，再入库，而不是把原始长文档直接扔进切分器机械处理
- 确实无法压缩概括、需要保留完整细节的内容（比如完整的用户协议条款），才采用递归式切分作为补充

### 7.3 Function Calling（工具调用）的使用

**核心思路**：不是所有问题都适合走 RAG 向量检索。像商品库存、价格、订单状态这类**实时变化的、需要100%精确**的数据，不适合存入向量库检索（大模型不擅长处理精确数字，向量库更新也跟不上数据实时变化），应该让 LLM 通过 Function Calling **调用具体的业务接口**去查询实时数据。

**判断标准**：
```
静态、不常变化的知识（政策、说明文档） → 走 RAG 检索
实时变化、要求精确的数据（库存、价格、订单状态） → 走 Function Calling 查接口
```

**LangChain4j 里的实现方式**（`@Tool` 注解）：
```java
public class ShopTools {
    @Tool("查询指定商品的库存数量")
    public int queryStock(String productId, String size) {
        return productService.getStock(productId, size);
    }

    @Tool("查询指定订单的物流状态")
    public String queryOrderStatus(String orderId) {
        return orderService.getStatus(orderId);
    }
}

ShopAssistant assistant = AiServices.builder(ShopAssistant.class)
        .chatModel(chatModel)
        .contentRetriever(faqRetriever)   // 处理通用政策类问题（RAG）
        .tools(new ShopTools())            // 处理实时数据查询（Function Calling）
        .chatMemoryProvider(chatMemoryProvider)
        .build();
```

LLM 会根据用户问题自动判断：是该走 RAG 检索知识库，还是该调用某个工具方法查实时数据，这种 **RAG + Function Calling 混合架构**，是目前智能客服类应用比较主流的做法。

**代码组织上的优化**：把不同业务领域的 `@Tool` 方法拆分到独立的工具类文件里（比如按商城、订单、库存等模块分别建立单独的 Tools 类），而不是全部堆在一个文件里，便于维护。

### 7.4 AI Agent 范式

Agent（智能体）是让 LLM 不只是"一问一答"，而是能够**自主规划、多步骤执行、必要时自我修正**去完成一个任务。常见的几种范式：

**1. ReAct（Reasoning + Acting，循环思考 + 调用工具）**
让 LLM 在"思考"和"行动"之间循环交替，每一步先输出一段推理过程，判断下一步该做什么，再决定调用哪个工具，拿到工具返回的结果后，继续思考下一步，如此循环，直到得出最终答案。
```
思考："用户想知道上周产量最低的产线，我需要先查数据"
    ↓ 行动：调用"查询产线产量"工具
    ↓ 观察：拿到工具返回的结果
思考："产线C最低，接下来我需要看看它有没有异常记录"
    ↓ 行动：调用"查询异常记录"工具
    ↓ 观察：拿到结果
思考："信息足够了，可以给出最终答案了"
    ↓ 输出最终回答
```
特点：每一步"思考"都会重新评估当前情况再决定下一步，比较灵活，适合任务路径不确定、需要根据中间结果动态调整下一步的场景。

**2. Plan-and-Solve（计划与执行）**
和 ReAct 边想边做不同，这种模式是**先一次性规划出完整的执行步骤清单，再按计划逐步执行**，执行过程中一般不会重新调整整体计划（除非引入额外的重规划机制）。
```
第一步：LLM 先输出完整计划
    "要回答这个问题，需要：① 查上周各产线产量 ② 找出最低的一条 ③ 查它的异常记录 ④ 综合分析"
    ↓
第二步：按照这个计划清单，依次执行每一步（可能调用工具，也可能是子任务）
    ↓
第三步：所有步骤执行完，汇总结果输出最终答案
```
特点：任务拆解更清晰、更可控，适合步骤相对明确、不需要频繁根据中间结果推翻重来的任务；相比ReAct，规划和执行是分离的两个阶段，不是每步都重新思考。

**3. Reflection（自我反思/自我纠正）**
让 LLM 在生成结果之后，**自己再检查、评估一遍自己的输出**，发现问题就修正，而不是一次生成就直接采用。
```
第一步：LLM生成初步答案/初步方案
    ↓
第二步：LLM切换到"审阅者"视角，检查这个答案有没有问题
   （比如：有没有编造内容、逻辑是否自洽、有没有遗漏用户要求的点）
    ↓
第三步：如果发现问题，重新生成或者针对性修正，可能反复几轮
    ↓
最终输出修正后的结果
```
特点：能有效降低幻觉、提升输出质量，但代价是至少多一次（甚至多轮）LLM调用，成本和耗时都会增加。这个思路和RAG部分提到的"生成后做忠实度自检"是同一个原理的应用。

**三种范式对比**：

| 范式 | 核心思路 | 适合场景 |
|---|---|---|
| ReAct | 边想边做，每步都重新评估 | 路径不确定、需要动态调整的任务 |
| Plan-and-Solve | 先规划完整步骤，再按计划执行 | 步骤相对明确、可预先拆解的任务 |
| Reflection | 生成后自我检查、修正 | 对输出质量、准确性要求高的场景，愿意为此多付出成本 |

**实际应用里通常会组合使用**，比如用 Plan-and-Solve 做整体任务拆解，每个子步骤内部再用 ReAct 的方式循环调用工具，最终输出前再加一轮 Reflection 自我检查，而不是三选一。

### 7.5 AI 记忆机制与多轮对话管理

**记忆和检索是两条独立的线**：记忆负责保存"历史对话内容"（谁问了什么、AI答了什么），检索负责"这一轮问题专属的参考资料"，每一轮都要重新检索，不会复用上一轮的检索结果，两者最终一起打包给 LLM。

**记忆容器的生命周期要正确管理**：`ChatMemory` 对象不能是接口方法里的局部变量（那样每次请求都会重新创建成一个空记忆，等于没有记忆），必须绑定在能跨越多次请求持续存活的地方（比如 Spring 单例 Bean 的成员变量），并通过 Map/Redis 按标识缓存查找，才能让记忆真正在多轮对话之间生效。

**记忆的成本控制**：对话轮数增加，历史记录会越叠越多，token 消耗跟着上涨，需要用窗口机制限制：
- `MessageWindowChatMemory`：按消息条数限制，只保留最近N条
- `TokenWindowChatMemory`：按token数限制，比按条数更精确
- 更高级的方案：定期对早期对话做摘要压缩，既控制成本又不完全丢失早期上下文

### 7.6 多个会话的区分设计

**核心是 `@MemoryId` 绑定的标识粒度选择**，根据业务场景不同，区分粒度也不同：

**1. 按用户ID区分（`user_id`）**
适合"同一用户同一时间只允许存在一个持续对话"的简单场景（比如一直续接的客服窗口，没有"新建对话"功能）。缺点是如果用户在不同时间问了完全不相关的话题，会全部堆在同一份记忆里，容易造成"记忆污染"（比如问完退货问题几周后又问别的，AI 却把之前退货的上下文也带进来干扰新问题）。

**2. 按会话ID区分（`session_id` / `conversation_id`），更常用、更推荐**
适合允许用户新建多个独立会话的场景（类似左侧有历史会话列表，可以新建对话、也可以点进某条历史继续聊）：
```
用户（user_id）
    ├── 会话1（session_id: abc-123）→ 独立的一份记忆
    ├── 会话2（session_id: def-456）→ 独立的一份记忆
    └── 会话3（session_id: ghi-789）→ 独立的一份记忆
```
需要一张会话表记录 session 和 user 的归属关系，并在接口层做权限校验（确认这个 session_id 确实属于当前登录用户，防止越权访问别人的会话）。

**3. 按机构ID（租户ID）做上一层隔离，是多租户 SaaS 场景必须加的维度**
如果系统是面向多个企业/机构的 SaaS 产品，隔离粒度不能只到"用户"这一层，还要再往上加一层"机构/租户"：
```
机构A（tenant_id / org_id）
    ├── 用户1 → 会话1、会话2...
    └── 用户2 → 会话1、会话2...
机构B（tenant_id / org_id）
    ├── 用户3 → 会话1...
```
这一层隔离不仅体现在记忆/会话上，也要体现在**知识库检索**上——检索时必须显式加上 `tenant_id` 的 metadata 过滤条件（向量检索本身不会自动按机构过滤），否则会出现跨机构的数据泄露风险（比如A机构的客服 AI 检索到了 B 机构的私密资料并回答给 A 机构用户）。

**实现层面的注意点**：
- 过滤/隔离逻辑建议封装到统一的服务层方法里，强制从登录上下文（ThreadLocal 或类似机制）里获取 `tenant_id`、`user_id`，不给业务代码"跳过隔离"的机会，避免因为个别代码遗漏加过滤条件导致数据泄露
- Redis 存储记忆时，key 也要把 `tenant_id` 拼进去（比如 `chat:memory:{tenant_id}:{session_id}`），即使 session_id 是 UUID 理论上不会重复，也要养成加前缀隔离的习惯

---

## 八、微服务架构（Nacos + OpenFeign）

### 8.1 Nacos（注册中心 + 配置中心）

**Nacos 的作用**：
- **服务注册与发现**：各个微服务启动后向 Nacos 注册自己的地址（IP+端口），其他服务/网关需要调用时，实时从 Nacos 查询目标服务当前有哪些可用实例，不需要写死地址
- **配置中心**：把各个环境（开发/测试/生产）的配置文件集中托管在 Nacos 上，支持**配置热更新**（修改配置后不需要重启服务，应用能实时感知变化），避免配置分散在各个服务自己的本地文件里，不好统一管理

**优点**：
- 注册中心和配置中心**二合一**，不需要像有些技术栈那样分别引入两个独立组件（比如 Eureka 只做注册发现，Config Server 只做配置管理），减少了要维护的组件种类
- 支持**动态感知服务实例变化**，配合负载均衡，天然支持弹性扩缩容场景（新增/下线实例，调用方能自动感知，不需要手动改配置重启）
- 配置**支持热更新**，改配置不用重启服务，运维效率高
- 国内中文文档、社区支持好（阿里开源），和Spring Cloud Alibaba生态集成度高，上手相对容易
- 支持**多环境、多命名空间**隔离配置（比如开发、测试、生产环境的配置可以在同一个Nacos里用命名空间区分管理，不用为每个环境单独搭一套）

**缺点**：
- 引入了额外的组件，增加了运维复杂度和部署成本（需要单独部署、维护Nacos本身的高可用）——服务数量不多、用Docker Compose部署、不需要动态扩缩容的场景下，直接写死路由配置可能比引入Nacos更简单
- Nacos本身也是一个"单点"，如果不做集群高可用部署，Nacos挂了会影响整个服务发现和配置读取的正常运作，需要额外投入做好Nacos自身的高可用
- 配置热更新虽然方便，但如果配置项设计或者代码里对配置变化的监听处理不当，也可能引入"运行时配置突然变化导致的不可预期行为"这类新的风险点，需要谨慎设计哪些配置支持热更新、哪些必须重启才生效

### 8.2 OpenFeign（服务间调用）

**作用**：让微服务之间的**远程HTTP调用**，写起来像调用本地方法一样简单，不需要自己手写 `RestTemplate`/`HttpClient` 拼接URL、处理序列化反序列化这些繁琐细节。

```java
@FeignClient(name = "auth-service")   // 指定要调用的目标服务名（可以配合Nacos做服务发现）
public interface AuthServiceClient {
    @PostMapping("/api/auth/login")
    ApiResult<LoginResponse> login(@RequestBody LoginRequest request);
}

// 调用方直接像调本地方法一样使用
authServiceClient.login(request);
```

**优点**：
- **代码简洁、声明式调用**，不用手写HTTP请求细节，接口定义清晰，调用方式和调本地Service方法几乎一样，降低了开发心智负担
- 天然**集成负载均衡**（配合注册中心，能在目标服务的多个实例之间自动做负载均衡）
- 可以配合**Hystrix/Sentinel**等组件做**熔断、降级**，服务调用失败时可以有兜底处理逻辑，不至于因为一个下游服务故障导致整条调用链路雪崩
- 支持**统一的拦截器机制**，方便统一处理鉴权头、日志记录等横切关注点

**缺点**：
- **本质仍是同步阻塞调用**（默认基于HTTP同步请求-响应），如果调用链路较长（服务A调服务B调服务C），容易出现**级联超时、性能瓶颈**问题，链路越长响应时间越可能叠加变长
- 服务间**强耦合**：调用方需要明确知道被调用方的接口定义（`@FeignClient`接口），如果被调用方接口发生变更，调用方代码也需要跟着改，不像消息队列那样天然解耦
- **默认调用没有做超时/重试的精细化配置**，需要额外手动配置超时时间、重试策略，否则某个下游服务响应慢，可能会拖慢甚至拖垮上游调用方（需要结合熔断降级组件一起使用，才能有比较完善的容错能力）
- 排查问题相对麻烦：因为是"看起来像本地方法调用"，如果不熟悉背后是走了网络请求，出问题时（比如超时、网络异常）容易一时想不到要去排查网络和下游服务状态

**小结**：Nacos解决的是"服务在哪里、配置是什么"的问题，OpenFeign解决的是"服务之间怎么方便地互相调用"的问题，两者搭配使用是Spring Cloud Alibaba技术栈里常见的组合——OpenFeign的`@FeignClient`指定服务名，实际调用时的地址解析可以依赖Nacos做的服务发现来完成。

---

## 九、Java 基础

### 9.1 Java 集合框架

**ArrayList**
- 底层结构：数组
- 查询：按下标查，速度快（O(1)，直接定位内存地址）
- 写：中间插入/删除较慢，因为需要把插入位置之后的所有元素依次往后（或往前）移动
- 扩容：默认扩容为原容量的 **1.5倍**，扩容时需要把原数组的数据整体复制到新数组里，这个复制过程比较消耗资源，所以能预估好初始容量的场景，最好提前指定容量，减少扩容次数

**LinkedList**
- 底层结构：双向链表
- 查询：慢，因为需要从头（或尾）开始按顺序遍历才能定位到目标位置，不能像数组一样直接按下标算出内存地址
- 写：快，插入/删除只需要改变前后节点的指针指向，不需要移动其他元素

**HashMap（JDK 1.8 之后）**

底层结构：数组 + 链表 + 红黑树

`put` 的完整流程：
```
1. 对 key 调用 hashCode() 得到初始 hash 值
2. 对这个 hash 值做位运算做"扰动"处理（h ^ (h >>> 16)），
   目的是减少哈希冲突（让高位信息也参与到低位的定位计算里，分布更均匀）
3. 如果是第一次put，先初始化数组（默认长度16，或者自定义指定的长度）
4. 用扰动处理后的 hash 值，计算出这个 key 应该落在数组的哪一个"桶"位置
5. 判断这个桶位置是否为空：
   - 为空 → 直接初始化一个新节点，放入这个桶
   - 不为空 → 判断桶里已有节点的 key 是否和当前要put的 key "相等"
     （相等的判断标准：hash值相同 且 对象值相同——对象值相同指的是
      == 比较引用相同，或者 equals() 比较值相同，两者满足其一）
6. 取出这个桶里的节点，判断节点当前是链表结构还是红黑树结构：
   - 红黑树 → 调用红黑树自身的put逻辑处理
   - 链表 → 用尾插法（JDK 1.8+），循环遍历链表：
     - 遍历过程中如果发现某个节点的 key 相等，跳出循环，走"值覆盖"逻辑
       （是否真正覆盖，取决于 putVal 方法的 onlyIfAbsent 参数：
        true 则不做替换操作，false 则用新值替换旧值）
     - 如果遍历到链表末尾都没有相等的 key，则插入到链表尾部
     - 链表默认最大长度是 8；如果这次插入使得链表达到第8个节点，
       并且此时数组的桶数量 > 64，则把这条链表转换成红黑树，
       提升这个桶位置后续的查询效率（红黑树查询是 O(logN)，链表是 O(N)）
7. 最后判断整个 HashMap 的元素个数（size）是否超过阈值
   （阈值 = 桶数量 × 加载因子，默认是 16 × 0.75 = 12），
   超过阈值就触发扩容，扩容后容量变为原来的 2倍
```

补充：JDK 1.7 及之前，链表插入用的是**头插法**（新节点直接插到链表头部），JDK 1.8 之后改为了**尾插法**，这个改动也是解决多线程扩容死循环问题的关键之一（见下文）。

**HashSet**
本质上是对 `HashMap` 的一层简单包装——`HashSet` 内部实际上维护了一个 `HashMap`，把每个存入的元素作为 `HashMap` 的 key，value 用一个固定的占位对象填充，所以底层的哈希计算、扩容、冲突处理逻辑基本和 HashMap 是一致的。

**HashMap 的线程安全问题**

HashMap 本身是线程不安全的（不加锁），多线程环境下并发操作容易出问题：
- **多线程并发 `put`**：容易出现相互覆盖，比如两个线程同时判断某个桶位置为空，都各自初始化节点尝试放入，后写入的会覆盖先写入的，导致数据丢失
- **扩容时的多线程数据迁移**：扩容需要把旧数组的数据重新计算位置迁移到新数组，多线程同时触发扩容容易导致数据丢失，甚至（在 JDK 1.7 用头插法的实现下）**因为多线程同时对链表做头插法迁移时，节点指针方向被并发操作意外反转，可能形成链表环，导致后续get操作陷入死循环**；JDK 1.8 改成尾插法之后，规避了这种链表成环导致死循环的问题，但**依然不能保证线程安全**，并发场景下仍然可能出现数据覆盖或丢失

**多线程场景应该使用 `ConcurrentHashMap`**：
- 通过 **CAS + synchronized** 结合的方式实现线程安全（CAS用于无锁的原子操作尝试，冲突时对具体的桶节点加 synchronized 锁，锁粒度比"锁住整个Map"要细得多，并发性能比早期简单粗暴地整体加锁的方案要好）
- **不允许 key 为 `null`，也不允许 value 为 `null`**：因为在并发场景下，如果通过 `get()` 拿到 `null`，无法区分到底是"这个key本身不存在"，还是"这个key存在，但对应的value本身就是null"这两种情况——普通HashMap单线程环境下还可以先用 `containsKey()` 二次确认来区分，但并发环境下这种"先判断再取值"的两步操作之间可能被其他线程插入修改，没办法保证判断的准确性，所以 ConcurrentHashMap 干脆直接不允许 key/value 为 null，从根源上避免这种歧义

**减少哈希冲突的关键手段：扰动函数**
`hash() = h ^ (h >>> 16)`，把 hashCode 的高16位和低16位做异或运算，让高位的信息也能参与到最终定位桶位置的计算中（如果不做这一步，数组容量较小时，只有hash值的低位参与了定位运算，高位信息完全被浪费，会导致更多key计算出相同的桶位置，冲突概率变高）。

**LinkedHashMap**

在 `HashMap` 的基础上，**额外维护了一个双向链表**，用来记录元素的顺序：
- **默认按插入顺序排列**：先放入的元素，遍历时也排在前面，插入顺序不会因为后续的读写操作而改变
- **可以配置成"访问顺序"模式**：每次 `get()` 访问某个元素，会把这个元素移动到链表的**尾部**（也就是"最近使用"的会被移到最后）
  ```
  初始顺序：a, b, c
  执行 get(a) 之后：b, c, a   （a 因为刚被访问过，移到了尾部）
  ```

LRU 缓存的实现原理：正是利用了 LinkedHashMap 这个"访问顺序"特性——每次访问都把元素移到链表尾部，那么链表**头部**自然就是"最久没有被访问"的元素；当缓存元素数量超过设定的容量上限时，只需要把链表头部的元素删除即可实现"淘汰最久未使用的数据"这个LRU核心逻辑。
```
当前缓存：a, b, c （c是最近访问的，排在最后；a最久没访问，排在最前）
新增 d，此时容量已满
    ↓ 淘汰链表头部（最久未使用的）a
最终缓存：b, c, d
```
这个思路也正好呼应了"四、缓存/Redis"部分的**淘汰策略里的 `allkeys-lru` / `volatile-lru`**——概念上是一致的，都是"淘汰最久未被使用的数据"，只是 Redis 的 LRU 是在缓存服务器层面实现的，而这里是用 Java 集合本身的特性手写实现一个简易的本地LRU缓存。

**TreeMap**
- 底层结构：红黑树
- 特点：按照 key 的顺序自动排序存储（默认自然顺序，也可以自定义比较器指定排序规则），**key 不能重复**（和 HashMap 一样是Map结构，key唯一）
- 因为底层是红黑树，查询、插入、删除的时间复杂度是 O(logN)，比 HashMap 的 O(1) 稍慢，但换来的是"始终保持有序"这个特性，适合需要**按key排序遍历**、或者需要**范围查找**（比如找比某个key大/小的最近元素）的场景

**TreeSet**
- 底层结构：红黑树（本质上底层是用 `TreeMap` 实现的，和 `HashSet` 基于 `HashMap` 包装是同样的思路——把元素作为 TreeMap 的 key 存入，value 用占位对象填充）
- 特点：
  - **自动排序**：插入顺序和遍历顺序无关，遍历时始终按元素本身的顺序输出（比如依次 `add(5)`、`add(1)`、`add(4)`，遍历结果是 `1, 4, 5`，而不是插入的顺序）
  - **去重**：和其他 Set 集合一样，不允许存入重复元素

**String / StringBuilder / StringBuffer 对比**
- **String**：**不可变对象**，任何"修改"操作（拼接、替换等）实际上都是**创建了一个新的字符串对象**，让引用重新指向这个新对象，原对象内容不会被改变。因为原对象本身不可变，所以**天然线程安全**（多个线程读同一个String对象不会有并发修改的问题）
- **StringBuilder**：内部维护一个可变的字符数组，"修改"操作是直接在这个数组上原地操作（追加、插入等），不需要像String那样每次都创建新对象，性能更好；但因为操作数组的过程没有加任何同步保护，**线程不安全**，多线程并发修改同一个StringBuilder实例可能出问题
- **StringBuffer**：和StringBuilder一样，内部也是维护可变字符数组、直接在数组上操作，但**给关键方法都加上了 `synchronized` 关键字**，保证了**线程安全**，代价是加锁带来的性能开销比StringBuilder略高

选择建议：字符串内容不需要频繁修改的场景，用String即可；单线程环境下频繁拼接/修改字符串，用StringBuilder（性能最好）；多线程环境下需要共享同一个字符串缓冲区并发修改，才需要用StringBuffer。

### 9.2 BIO / NIO / AIO

- **BIO（Blocking I/O，同步阻塞IO）**：基于 `Stream`（流），一个连接对应一个线程，读写数据时线程会**阻塞等待**，直到数据准备好/写完为止，这段时间线程什么也做不了。连接数一多，需要的线程数也跟着线性增长，高并发场景下线程资源开销很大
- **NIO（Non-blocking I/O，同步非阻塞IO）**：基于 `Channel`（通道）+ `Buffer`（缓冲区），配合**多路复用器（Selector）**，一个线程可以同时管理多个连接，不需要阻塞等待某一个连接的数据，而是轮询/监听哪些连接已经就绪可以读写，就绪了再去处理，大幅减少了所需的线程数量，适合高并发场景（这也是ES的I/O多路复用"epoll等，单线程也能高并发处理网络请求"背后同一类原理的体现）
- **AIO（Asynchronous I/O，异步非阻塞IO）**：发起读写请求后，线程不需要等待也不需要轮询，直接返回去做别的事情，等操作系统完成实际的读写操作后，会**主动回调**通知应用程序处理结果。理论上比NIO更进一步，但在Linux系统上的底层实现和性能表现不如NIO成熟，实际应用中不如NIO常见（Java的Netty等主流高性能网络框架大多还是基于NIO来实现的）

### 9.3 深拷贝 vs 浅拷贝

- **浅拷贝**：只拷贝对象本身的引用/基本类型字段，如果对象内部还包含其他对象类型的字段，浅拷贝后这些字段依然**指向原来的同一个对象**，没有创建新的对象副本——也就是说，修改拷贝后的对象里那个"内部对象字段"，会影响到原对象（因为两者指向的是同一个内部对象）
- **深拷贝**：不仅拷贝对象本身，如果内部包含其他对象类型的字段，也会**递归地为这些内部对象创建全新的对象**，拷贝后的对象和原对象**完全独立**，修改任何一方都不会互相影响

### 9.4 反射（Reflection）

在运行时动态获取一个类的结构信息（字段、方法、构造器等），并且能动态创建对象、调用方法、访问/修改字段，而不需要在编译期就明确知道具体的类。是很多框架（比如Spring的依赖注入、各种ORM框架）实现的底层基础技术。

### 9.5 多线程实现方式

- **继承 `Thread` 类**：重写 `run()` 方法，没有执行结果返回（`run()` 方法本身是 `void`）
- **实现 `Runnable` 接口**：同样重写 `run()` 方法，没有执行结果返回，相比继承Thread的方式，因为Java是单继承，实现接口的方式更灵活（可以同时再继承别的类）
- **实现 `Callable` 接口**：重写 `call()` 方法，**有执行结果返回**（配合 `Future` 可以拿到异步执行的结果，也能捕获执行过程中抛出的异常）
- **线程池创建**：不是直接new线程，而是通过线程池来复用线程资源，避免频繁创建销毁线程的开销，也能更好地控制并发线程数量，是实际项目里更推荐的方式

### 9.6 线程的 6 种状态

- **NEW**：线程对象已创建，但还没有调用 `start()` 方法
- **RUNNABLE**：调用了 `start()` 后，处于可运行状态（包括正在CPU上实际执行、以及等待CPU调度这两种情况，Java把这两种都归为RUNNABLE，不像操作系统层面会细分"就绪"和"运行中"）
- **BLOCKED**：线程正在等待获取一个**监视器锁**（比如等待进入 `synchronized` 代码块/方法），处于阻塞状态
- **WAITING**：线程**无限期**等待另一个线程执行某个特定操作才能继续（比如调用了 `Object.wait()`、`Thread.join()` 且不带超时参数）
- **TIMED_WAITING**：和WAITING类似，但是**带了超时时间**的等待（比如 `Thread.sleep(时间)`、带超时参数的 `wait(时间)`），超时后会自动恢复，不需要一定等到别的线程唤醒
- **TERMINATED**：线程执行完毕（正常结束或者抛出异常终止），生命周期结束

### 9.7 JMM（Java 内存模型）

Java 内存模型定义了多线程环境下，各个线程之间**如何看待彼此对共享变量的修改**，主要有两种并发模型思路：
- **消息传递并发模型**：线程之间没有共享数据，通过显式发送消息来通信和同步（比如Actor模型），线程之间相对独立，不容易出现共享数据的并发安全问题，但需要额外的消息传递机制
- **共享内存并发模型（Java 采用的模型）**：线程之间通过读写**共享的内存变量**来隐式进行通信，这也是为什么Java里会有各种"多线程并发安全问题"（比如HashMap线程不安全），需要靠 `synchronized`、`volatile`、各种原子类、锁机制等手段来保证多线程访问共享变量时的可见性、有序性、原子性

### 9.8 并发问题的根源：原子性、可见性、有序性

多线程并发出现各种bug，本质上都能归结到下面这三个特性没有被保证：

- **原子性**：一个操作（或一系列操作）要么全部执行完成，要么完全不执行，中间不会被其他线程插入打断。比如 `i++` 这种看起来是一步的操作，实际上底层拆成了"读取i → 计算i+1 → 写回i"三个步骤，多线程并发执行时，完全可能在中间被其他线程插队，导致最终结果和预期不符
- **可见性**：一个线程对共享变量的修改，其他线程能不能**立刻**看到。由于CPU有各自的高速缓存，线程操作变量时可能先读到/改到自己缓存里的副本，没有及时同步回主内存，导致其他线程看到的还是旧值
- **有序性**：代码的实际执行顺序，可能和编写时的书写顺序不一致——编译器和CPU为了优化性能，会在不影响单线程执行结果的前提下对指令做**重排序**，但这种重排序在多线程场景下可能会破坏程序原本预期的逻辑顺序，导致意料之外的结果

这三者是理解 `synchronized`、`volatile`、各种锁存在意义的核心出发点——它们各自解决的就是这三个问题中的一个或多个。

### 9.9 互斥同步 vs 非阻塞同步

**互斥同步（悲观锁思路）**：假设一定会发生并发冲突，所以访问共享资源前先加锁，同一时刻只允许一个线程进入，其他线程只能阻塞等待锁被释放。

- **`synchronized`**：JVM层面内置的关键字，自动加锁、自动释放锁（不需要手动写释放逻辑，即使抛异常也会自动释放），属于**非公平锁**
- **`ReentrantLock`**：JUC包提供的显式锁，**需要手动加锁（`lock()`）和手动释放锁（`unlock()`，必须放在 `finally` 块里保证一定会执行）**，功能比 `synchronized` 更丰富（支持公平锁、可中断、可以 `tryLock()` 尝试获取锁而不阻塞等待）

两者共同点：**都会导致线程阻塞、都是悲观锁思路**——线程拿不到锁就只能挂起等待，无法继续做其他事情。

**非阻塞同步（乐观锁思路：CAS）**：假设大多数情况下不会发生冲突，不加锁，先尝试直接修改，如果发现修改期间被别人抢先改过了，就重试，而不是一开始就阻塞等待。

**CAS（Compare And Swap）** 涉及三个值：
- **V**：内存地址中实际存储的当前值
- **A**：线程预期的旧值（读取时记录下来的）
- **B**：想要更新成的新值

执行逻辑：只有当**内存地址V里的值，仍然等于线程预期的旧值A时**，才会把V更新为新值B；如果这期间V已经被其他线程改过了（V ≠ A），说明有竞争，本次更新失败。

**竞争失败后的处理：自旋重试**——不是放弃，而是重新读取一次最新的值，再次尝试CAS更新，循环往复，直到成功为止（这也是为什么叫"非阻塞"，线程一直在自己重试，没有被挂起阻塞）。

**CAS 的 ABA 问题**：
```
线程1：读取到内存值为 A
线程2：把值从 A 改成 B，又改回 A
线程1：这时候再执行CAS，发现内存值"看起来"还是A，认为没有被修改过，于是更新成功
```
但实际上这段时间值确实被改动过（A→B→A），只是线程1完全感知不到中间发生过的变化，如果业务逻辑依赖"值有没有被改过"这个判断，就可能出问题。**解决思路**：给值加上一个版本号（或者时间戳），每次修改版本号都递增，判断时不仅比较值本身，还要比较版本号是否一致，这样即使值被改回了原样，版本号也回不去，能识别出"中间确实发生过变化"。

### 9.10 锁的补充：synchronized 与 volatile

**`synchronized` 的三种使用方式**：
- **修饰普通（实例）方法**：锁的是**当前实例对象**（`this`），不同实例对象之间互不影响，各自独立加锁
- **修饰静态方法**：锁的是**当前类的Class对象**，因为静态方法属于类而不属于某个具体实例，所有实例共享同一把锁
- **修饰代码块（锁指定对象）**：`synchronized(obj)`，锁的是括号里指定的这个对象，可以更精细地控制加锁范围，减小锁粒度，不用把整个方法都锁住

`synchronized` 能够同时保证**原子性、可见性、有序性**三个特性——加锁范围内的代码不会被其他线程打断（原子性），解锁时会把修改刷新回主内存、其他线程加锁时会重新从主内存读取最新值（可见性），同时也限制了指令重排序不能越过锁的边界（有序性）。默认是**非公平锁**（不保证等待时间最久的线程一定优先获取到锁，允许后来的线程"插队"抢锁，减少线程切换开销）。

**`volatile` 关键字**：
- 保证**可见性**：修饰的变量一旦被某个线程修改，会立刻同步刷新到主内存，其他线程读取时也会直接去主内存读最新值，不会读到自己缓存里的旧值
- 保证**有序性**：通过插入内存屏障，**禁止指令重排序**，避免因为重排序破坏多线程场景下的逻辑顺序
- **不能保证完全的原子性**：只能保证**单次**的读操作或写操作是原子的，但像 `i++` 这种"读取+计算+写回"的复合操作，`volatile` 没法保证整体的原子性，中间依然可能被其他线程插入打断，这种场景要保证原子性还是得用 `synchronized`、`ReentrantLock`，或者JUC提供的原子类（如 `AtomicInteger`，底层就是用CAS实现的）

---

## 十、JVM

### 10.1 类的加载方式

1. **命令行启动应用时，由JVM初始化加载**：应用启动入口类，随着JVM启动过程被自动加载、初始化（会执行静态代码块/静态变量赋值）
2. **通过 `Class.forName()` 方法动态加载**：默认会执行类的静态方法（静态代码块），如果调用时额外指定了参数（比如指定不初始化，或者指定不同的类加载器），可以控制不执行静态方法/静态代码块
3. **通过 `ClassLoader.loadClass()` 方法动态加载**：默认**不会**执行类的静态方法/静态代码块，只是把类加载到JVM里，真正用到（比如实例化、调用静态成员）的时候才会触发初始化

### 10.2 类加载的完整过程与类加载器

**类加载的完整过程**：`加载 → 连接（验证 → 准备 → 解析） → 初始化`，之后如果代码中真正 `new` 对象，才会进入**实例化**阶段。
- **加载（Loading）**：找到对应的 `.class` 字节码文件，并读取进内存。
- **连接（Linking）**：
  - **验证**：检查字节码文件格式是否合法、是否符合 JVM 规范，防止不安全或不正确的代码危害 JVM 自身。
  - **准备**：为类的静态变量分配内存，并赋予默认初始值（比如 `int` 默认是 0，不是代码里写的实际初始值）。
  - **解析**：把类中的符号引用替换为直接引用（比如方法调用从"引用某个方法的描述符号"变成"引用实际的内存地址"）。
- **初始化（Initialization）**：真正执行类的静态代码块、给静态变量赋予代码里指定的实际初始值（呼应前面 `Class.forName()` 默认会触发这一步，`ClassLoader.loadClass()` 默认不会）。
- **实例化（Instantiation）**：类初始化完成之后，代码中执行 `new` 关键字，才会真正创建这个类的对象实例。这一步和"类加载"是两个不同阶段的概念：类只需要加载一次，但可以基于这个类创建很多个对象实例。

**类加载器的种类**（按加载范围从核心到自定义，形成父子委托关系）：
- **启动类加载器（Bootstrap ClassLoader）**：加载 JVM 核心类库（比如 `java.lang` 包下的基础类），是最顶层的加载器，由 C++ 实现，属于 JVM 自身的一部分。
- **扩展类加载器（Extension ClassLoader）**：加载 Java 的扩展类库。
- **应用程序类加载器（Application ClassLoader）**：加载我们自己写的应用程序代码，以及项目依赖的第三方 jar 包，是平时开发中接触最多的默认类加载器。

**双亲委托机制**：加载一个类时，会先逐级向上委托给父加载器尝试加载，只有父加载器加载不了，才轮到自己去加载。这样能保证核心类库不会被自定义的类随意覆盖替换，保障 JVM 运行的安全性和一致性。

### 10.3 JVM 运行时数据区

**线程私有**（每个线程各自独立一份，互不共享）：
- **程序计数器**：记录当前线程执行到字节码的哪一行，是线程切换后恢复执行位置的依据
- **虚拟机栈**：保存方法的局部变量、部分中间结果，并参与方法的调用和返回（每个方法调用对应一个栈帧，入栈出栈）
- **本地方法栈**：作用和虚拟机栈类似，只是服务于 `native` 方法（调用本地C/C++实现的方法）

**线程共享**（所有线程共用同一份）：
- **堆**：存放对象实例，是垃圾回收的主要区域
- **方法区 / 元空间**：存放类的结构信息（字段、方法、常量池等），元空间是方法区在 HotSpot 虚拟机里的具体实现（Java 8 之后用元空间替代了之前的永久代）

#### 堆内存分代与对象晋升

**堆的典型分代结构**：年轻代（新生代）+ 老年代。
- **年轻代 : 老年代 = 1 : 2**：可以通过 `-XX:NewRatio=2` 配置。这里的 `NewRatio` 表示"老年代 / 年轻代"的比例，所以值为 2 时，老年代是年轻代的 2 倍。
- **Eden : From Survivor : To Survivor = 8 : 1 : 1**：可以通过 `-XX:SurvivorRatio=8` 配置。这里的 `SurvivorRatio` 表示 Eden 和**单个** Survivor 区的比例。
- **自适应调整**：如果开启了 `-XX:+UseAdaptiveSizePolicy`，JVM 会根据运行情况动态调整堆中各区域大小，以及对象晋升老年代的年龄阈值，此时手动配置的 `-XX:NewRatio`、`-XX:SurvivorRatio` 不一定严格生效。JDK 8 中吞吐量优先的收集器默认会开启该策略。

**Minor GC 的触发和对象移动**：
- 当 Eden 区空间不足，无法继续为新对象分配内存时，会触发年轻代垃圾回收，也就是 **Minor GC**。
- Minor GC 后仍然存活的对象，会被复制到 Survivor 区；对象每经历一次 Minor GC 并存活下来，年龄就加 1。对象年龄由对象头中的年龄计数记录，最大晋升年龄可以通过 `-XX:MaxTenuringThreshold` 配置，默认最大值通常是 15。
- Survivor 区采用 From/To 两块轮换使用：每次 Minor GC 时，把 Eden 和当前 From Survivor 中存活的对象复制到 To Survivor；下一次 GC 时，From 和 To 的角色互换。
- 当对象年龄达到晋升阈值，或者 Survivor 区放不下存活对象时，对象会进入老年代。大对象也可能通过 `-XX:PretenureSizeThreshold` 直接进入老年代（具体是否生效和垃圾收集器有关）。
- 老年代空间不足时，会触发老年代相关的回收（常被称为 Major GC，很多场景也会伴随 Full GC）。如果回收后仍然无法为对象腾出足够空间，就可能抛出 `OutOfMemoryError`。

**方法区 / 永久代 / 元空间**：
- 方法区存放已经被虚拟机加载的类型信息、运行时常量池、静态变量、即时编译器编译后的代码缓存等。
- Java 7 及之前，HotSpot 使用**永久代（PermGen）**实现方法区，可以通过 `-XX:PermSize` 和 `-XX:MaxPermSize` 设置永久代大小。
- Java 8 之后，永久代被移除，改为使用**元空间（Metaspace）**，永久代相关参数随之失效；元空间使用本地内存，常用 `-XX:MetaspaceSize` 和 `-XX:MaxMetaspaceSize` 控制。

### 10.4 执行引擎

执行引擎负责把加载进 JVM 的字节码，真正翻译/执行成机器可以运行的指令，主要包含：
- **解释器（Interpreter）**：逐行解释执行字节码指令，启动快，但执行效率相对较低，因为每次执行都要重新解释一遍。
- **即时编译器（JIT，Just-In-Time Compiler）**：对于被反复频繁执行的"热点代码"，JIT 会把这部分字节码**直接编译成本地机器码**并缓存起来；之后再执行到这段代码时，可以直接运行编译好的机器码，不用再逐行解释，大幅提升执行效率。这也是 Java "既有解释执行的灵活启动速度，又能通过 JIT 达到接近编译型语言执行效率"的关键机制。
- **垃圾回收器（Garbage Collector）**：负责自动管理内存，回收不再被使用的对象。下面的可达性分析算法、GC Roots、垃圾回收算法和垃圾收集器，就是它判断和回收对象时依赖的重要知识点。

### 10.5 判断对象是否可以被回收

**主要采用可达性分析算法（Reachability Analysis）**：
```
从一系列称为 GC Roots 的根对象出发，沿着引用链向下搜索，
能被搜索到的对象，说明还"可达"，不能被回收；
搜索完之后，和 GC Roots 之间没有任何引用链能连接到的对象，判定为不可达，可以被回收
```

**常见的 GC Roots 包括**：
- 虚拟机栈中（栈帧的本地变量表）引用的对象
- 方法区中静态属性引用的对象
- 方法区中常量引用的对象
- 本地方法栈中引用的对象

**另一种方式：引用计数法**（作为对比了解，Java没有采用这种方式）：给每个对象维护一个引用计数器，每被引用一次计数+1，引用失效计数-1，计数为0就判定可以回收。缺点是**无法解决循环引用问题**（比如对象A引用对象B，对象B又引用对象A，即使两者都已经没有被外部任何地方引用了，彼此的引用计数依然不为0，导致本该被回收的垃圾对象永远无法被回收），这也是Java选择用可达性分析算法而不是简单引用计数法的原因。

**`finalize()` 方法**：对象真正被回收前，JVM 可能会调用对象的 `finalize()` 方法，给对象一次"自救"机会。但它和 `finally` 不是一回事：`finally` 是异常处理流程里一定会尝试执行的代码块，而 `finalize()` 依赖 GC 触发，执行时机不确定，也不推荐在实际业务里依赖它做资源释放。

**引用类型**：
- **强引用**：最常见的普通对象引用，只要强引用还存在，对象就不会被回收。
- **软引用**：内存不足时才可能被回收，适合做一些可有可无的缓存。
- **弱引用**：只要发生 GC，不管内存是否充足，只要对象只剩弱引用，就会被回收。
- **虚引用**：不能通过它直接获取对象，主要用于跟踪对象被回收的状态，通常需要配合引用队列使用。

#### 垃圾回收算法

- **标记-清除算法（Mark-Sweep）**：先从 GC Roots 出发标记所有可达对象，没有被标记的对象就认为是垃圾并清除。缺点是清除出来的内存空间不连续，容易产生**内存碎片**，后续分配大对象时可能明明总剩余空间够，却找不到一块连续空间。
- **标记-整理算法（Mark-Compact）**：先标记存活对象，然后把存活对象统一向内存的一端移动，最后清理边界以外的内存。优点是回收后空间连续，适合老年代这种对象存活率较高的区域。
- **标记-复制算法（Copying）**：把内存分成两块，每次只使用其中一块；GC 时把存活对象复制到另一块空闲区域，然后直接清空当前使用的整块内存。优点是实现简单、没有碎片，缺点是如果存活对象很多，复制成本会变高，并且需要预留一部分空间。
- **分代收集算法**：不是一种新的基础算法，而是把上面几种思想按不同代的特点组合使用。新生代对象"朝生夕死"，存活率低，适合用复制算法；老年代对象存活率高，适合用标记-清除或标记-整理算法。

#### 垃圾收集器

- **Serial 收集器**：单线程串行收集器，GC 时会暂停所有用户线程（STW），简单高效，适合单核或小内存场景。
- **ParNew 收集器**：可以看作 Serial 新生代收集器的多线程版本，默认线程数通常与 CPU 数量相关，也可以通过 `-XX:ParallelGCThreads` 指定 GC 线程数。
- **Parallel Scavenge 收集器**：新生代收集器，更关注**吞吐量**。吞吐量 = 用户代码运行时间 /（用户代码运行时间 + GC 时间），适合后台计算、批处理这类更看重整体吞吐的场景。
- **Serial Old 收集器**：Serial 的老年代版本，使用标记-整理算法，也是单线程收集器。
- **Parallel Old 收集器**：Parallel Scavenge 的老年代版本，使用多线程标记-整理算法，和 Parallel Scavenge 搭配时可以形成一套吞吐量优先的组合。
- **CMS 收集器（Concurrent Mark Sweep）**：老年代收集器，核心目标是减少停顿时间，主要使用标记-清除算法。流程是：初始标记（标记 GC Roots 直接关联对象，速度快，需要 STW）→ 并发标记（沿引用链标记可达对象，和用户线程并发执行）→ 重新标记（修正并发期间引用变化导致的标记变动，需要 STW）→ 并发清除（清理不可达对象，和用户线程并发执行）。因为 CMS 使用标记-清除算法，所以会产生内存碎片。
- **G1 收集器（Garbage First）**：把堆划分成多个大小相等的 **Region**，启动时可以用 `-XX:G1HeapRegionSize=n` 指定 Region 大小（1MB~32MB，且必须是 2 的幂），JVM 默认会根据堆大小尽量把整堆划分成约 2048 个 Region。Region 内部还会再划分为更小的 Card，用来记录跨 Region 引用，降低全堆扫描成本。G1 会优先回收垃圾比例高、回收收益大的 Region，所以叫 Garbage First。

**CMS 和 G1 的一个核心区别**：CMS 主要是标记-清除，清理后可能留下碎片；G1 以 Region 为单位做回收，整体思路更偏向"标记 + 复制/整理"，把存活对象复制到其他 Region，再清空原 Region，因此更容易得到连续可用空间，碎片问题更少。

**G1 的大致流程**：
```
初始标记（标记 GC Roots 直接引用的对象，需要 STW）
    ↓
并发标记（标记从 GC Roots 可达的全部对象，和用户线程并发执行）
    ↓
最终标记 / 重新标记（修正并发期间引用变化，需要 STW）
    ↓
筛选回收（根据每个 Region 的垃圾比例和回收收益，优先回收价值高的 Region）
```

#### 内存分配与晋升策略

- **对象优先在 Eden 分配**：大多数新对象会先分配在 Eden 区，Eden 空间不足时触发 Minor GC。
- **大对象直接进入老年代**：需要大量连续内存的大对象（比如很长的数组、字符串）可能直接进入老年代，可以通过 `-XX:PretenureSizeThreshold` 设置阈值，但该参数是否生效和具体垃圾收集器有关。
- **长期存活对象进入老年代**：对象每经历一次 Minor GC 并存活下来，年龄加 1；当年龄达到 `-XX:MaxTenuringThreshold` 设置的阈值后，就会晋升到老年代。
- **动态年龄判断**：如果 Survivor 区中相同年龄对象的大小总和大于 Survivor 空间的一半，那么年龄大于或等于该年龄的对象可以直接进入老年代，不必等到 `MaxTenuringThreshold`。
- **空间分配担保**：Minor GC 前，JVM 会检查老年代是否有足够空间接纳年轻代可能晋升的对象；如果担保失败，可能提前触发 Full GC，或者在 Minor GC 后因为晋升失败再触发更重的回收。

#### Full GC 常见触发场景

- **老年代空间不足**：比如空间分配担保检查失败、Minor GC 后对象晋升到老年代失败、或者大对象需要连续空间但老年代放不下。
- **方法区 / 元空间不足**：类信息、常量、元数据等占用过多，元空间无法继续分配时可能触发 Full GC；回收后仍不足则可能抛出元空间相关的 OOM。
- **主动调用 `System.gc()`**：代码显式建议 JVM 执行 GC，通常会触发 Full GC（只是建议，不保证一定立即执行）。实际项目中一般不建议随意调用。
- **CMS 的 `Concurrent Mode Failure`**：CMS 并发清理过程中，用户线程还在继续运行并产生新对象，如果老年代对象分配速度超过 CMS 清理速度，CMS 会被迫放弃并发清理，退回到 Serial Old 进行单线程回收，产生较长时间 STW。
- **CMS 的 `Promotion Failed`**：Minor GC 时存活对象要晋升老年代，但老年代没有足够连续空间，导致晋升失败，可能触发 Full GC。
- **老年代空间碎片过多**：总剩余空间看起来够，但连续空间不足，无法分配大对象，也可能触发 Full GC。
- **G1 并发失败**：G1 在并发标记或回收过程中，如果对象分配速度过快、回收跟不上，也可能退化为 Full GC。

---

## 待深入 / 后续可以扩展的方向

- [ ] ES 分页的具体实现方式（from+size 方案 vs search_after 方案，数据量大时的深分页性能问题）
- [ ] 缓存穿透/缓存击穿的处理（如果 ES 也查不到，是否需要缓存空结果，防止大量无效查询打到 ES）
- [ ] ES 与 MySQL 同步机制的具体技术选型细节（Canal 配置、双写方案对比等）

