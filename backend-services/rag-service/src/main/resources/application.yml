server:
  port: ${RAG_SERVICE_PORT:8092}

spring:
  application:
    name: rag-service

rag:
  max-results: ${RAG_MAX_RESULTS:5}

management:
  endpoints:
    web:
      exposure:
        include: health,info
